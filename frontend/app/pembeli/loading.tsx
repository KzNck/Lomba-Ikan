import { MainSkeleton } from '@/components/dashboard/main-skeleton'

// Shown for every pembeli page while it loads.
export default function PembeliLoading() {
  return <MainSkeleton role="pembeli" />
}
