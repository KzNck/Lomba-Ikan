import { MainSkeleton } from '@/components/dashboard/main-skeleton'

// Riwayat's filters and table, while the history loads.
export default function NelayanRiwayatLoading() {
  return <MainSkeleton role="nelayan" shape="table" />
}
